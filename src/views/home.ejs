<%- include('partials/header') %>

<main class="container mt-4">
  <div class="jumbotron text-center">
    <h1 class="display-4">Bienvenido</h1>
    <p class="lead">Empleados | Home</p>
    <hr class="my-4">
    <%- include('partials/flashMessages') %>
  </div>

</main>
<% if (user) { %>
  <div class="card">
    <div class="card-header">
      <h3>Listado de Empleados</h3>
    </div>
    <div class="card-body">
      <div class="table-responsive custom-table-width mx-auto">
        <%- include('empleado/listarEmpleados', { empleados: empleados }) %>
      </div>
    </div>
  </div>
<% } else { %>
  <div class="text-center">
    <p class="lead">Por favor, inicie sesión para ver el listado de empleados.</p>
  </div>
<% } %>

<%- include('partials/footer') %>
