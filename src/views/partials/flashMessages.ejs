<script>
  <% if (messages.error && messages.error.length > 0) { %>
    showSwal('Error', '<%= messages.error[0] %>', 'error');
    toastr.error('<%= messages.error[0] %>');
  <% } %>

  <% if (messages.success && messages.success.length > 0) { %>
    showSwal('Éxito', '<%= messages.success[0] %>', 'success');
    toastr.success('<%= messages.success[0] %>');
  <% } %>
</script>